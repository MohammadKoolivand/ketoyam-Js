import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
  rootLogin: {
    display: 'flex',
    width: 200,
    height: 200,
    margin: 'auto',
    justifyContent: 'center',
    backgroundColor: '#fff',
    alignItems: 'center',
    marginTop: 50,
    outline: 'none'
  }
}))
